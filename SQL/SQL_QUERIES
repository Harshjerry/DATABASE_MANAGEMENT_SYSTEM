USE org;
SELECT FIRST_NAME AS WORKER_NAME FROM Worker;
SELECT UPPER(FIRST_NAME) FROM worker; 
SELECT * FROM  worker;
SELECT DISTINCT DEPARTMENT FROM worker;
SELECT DEPARTMENT,COUNT(DEPARTMENT) FROM worker GROUP BY DEPARTMENT;

-- Print  first three characters of FIRST_NAME OF WORKER
SELECT substr(FIRST_NAME,1,3) FROM worker;

SELECT  instr(FIRST_NAME,'B') FROM  worker WHERE  FIRST_NAME='Amitabh';
--  output wil be    position of alphabet b in  first_nsme col amitabh

-- get  uniwu value of depratment  and print its length
SELECT DISTINCT DEPARTMENT , length(DEPARTMENT) FROM worker ;

-- REPLACITNG   'a' in  first_name with 'A';
SELECT REPLACE(FIRST_NAME,'a','A') FROM worker;

SELECT * FROM worker ORDER BY SALARY DESC LIMIT 5;

-- TO GET  5TH  SALARY    we use below command  where offset 4 and 1 
SELECT * FROM worker ORDER BY SALARY DESC LIMIT 4,1;


 SELECT * FROM worker AS W INNER JOIN title as T ON W.WORKER_ID = T.WORKER_REF_ID;
 
 --   employees with same salary
 SELECT W.* FROM worker AS W INNER JOIN worker as T ON W.SALARY = T.SALARY  WHERE W.WORKER_ID != T.WORKER_ID;


SELECT * FROM worker;
SELECT * FROM title;
INSERT INTO title (WORKER_REF_ID,WORKER_TITLE) VALUES
(1,'Lead');
SELECT * FROM bonus;

-- WORKERS NOT GETTING BONUS
SELECT * FROM  worker AS W WHERE W.WORKER_ID NOT IN ( SELECT WORKER_REF_ID FROM Bonus );


-- fetch    half records from table
SELECT  * FROM worker WHERE WORKER_ID<=(SELECT COUNT(worker_id)/2 FROM worker);
 
 -- department with  less than  4 people -- remember when we use aggregation or grouping conditions are applied by HAVING BY 
        SELECT DEPARTMENT, COUNT(WORKER_ID)as c FROM worker GROUP BY DEPARTMENT HAVING c>=4;
 
 




